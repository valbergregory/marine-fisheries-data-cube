# Registro de decisões

| ID | Data | Decisão | Justificativa | Status |
|---|---|---|---|---|
| D1 | 2026-09-03 | Projeto criado em subdiretório próprio `marine-fisheries-data-cube` (nome sem espaços/acentos) dentro de `D:\Claude code - projetos` | O diretório raiz contém vários projetos independentes; nomes sem espaço evitam fricção com CLI/renv/Quarto/Python | ativa |
| D2 | 2026-09-03 | Unidade de análise preferida: célula espacial × **mês**; comparação formal H3 res. 4/5/6 vs. grade regular 0,25°, e mês vs. semana, adiada para o piloto | Compatibilidade com OISST 0,25°, controle de zeros excessivos e custo; critérios: % células com esforço>0, variância, custo de processamento | pendente de dados do piloto |
| D3 | 2026-09-03 | Dependências em dois estágios: `Imports` (viabilidade: targets, yaml, jsonlite, httr2, curl, data.table, checkmate, tarchetypes) instaladas agora; stack espacial/pesada (`Suggests`) instalada só após aprovação da viabilidade | Regra do protocolo: não instalar tudo automaticamente; evita horas de instalação antes de saber se o projeto é viável | ativa |
| D4 | 2026-09-03 | SST via NCEI HTTPS (arquivos diários) com OPeNDAP/PSL para recortes; ERDDAP rebaixado a alternativa | ERDDAP indisponível no teste (HTTP 000); NCEI e PSL responderam 200 | ativa |
| D5 | 2026-09-03 | Definição de MHW: Hobday et al. (2016) via heatwaveR; parâmetros em `config/config.yml`; anomalia simples nunca rotulada como MHW | Referência canônica confirmada via Crossref; implementação auditada em pacote publicado (JOSS) | ativa |
| D6 | 2026-09-03 | Linguagem associacional em todo o texto até identificação defensável; plausibilidade causal argumentada, não afirmada | Regra 12 do protocolo do pesquisador | ativa |
| D7 | 2026-09-03 | `gfwr` instalado do GitHub com versão pinada no renv.lock (não está no CRAN — verificado 404) | Cliente oficial da GFW; pinagem preserva reprodutibilidade | ativa |
| D8 | 2026-09-03 | CRS: análise métrica em EPSG:5880 (Brazil Polyconic/SIRGAS 2000); armazenamento geodésico em EPSG:4674 | Distâncias/áreas em metros ao longo de toda a costa; padrão SIRGAS do IBGE | ativa |
| D9 | 2026-09-03 | Python somente se necessário (candidato único até agora: toolbox `copernicusmarine` para clorofila — extensão); econometria permanece em R | Regra da arquitetura R-first | ativa |
| D10 | 2026-09-04 | OISST do piloto via THREDDS/NCSS da PSL em **janelas semestrais** (`accept=netcdf`), lidas com `ncdf4` (adicionado a Imports; já estava no lock via rerddap) | Janela anual devolve HTTP 502; ERDDAP segue fora do ar; GDAL/terra corrompe a georreferência do recorte NCSS (extent inválido), então coordenadas são lidas direto do NetCDF | ativa |
| D11 | 2026-09-04 | Piloto baixa SST diária 1991–2023 (33 anos) do bbox NE para calcular MHW pela definição Hobday completa já no piloto (climatologia 1991–2020; 2021 incluído para continuidade da série) | Evita rotular anomalia simples como MHW (D5); custo baixo porque o recorte NE é ~450 KB/semestre | ativa |
| D12 | 2026-09-04 | Universo do piloto = células H3 res 5 do bbox com centróide sobre pixel OISST oceânico; se >400, amostra aleatória com `seed` do config; esforço GFW mapeado por `point_to_cell` dos centros 0,1° | Regra do protocolo (≤400 células) sem viesar o universo para células com esforço — necessário para responder a pergunta 5 honestamente | ativa |
| D13 | 2026-09-07 | OISST do piloto via **OPeNDAP (dodsC) da PSL** com recorte por índices no servidor, lido com `ncdf4` (validado no Windows em 07/09), cache anual em RDS com checksum; substitui o NCSS de D10 | NCSS da PSL passou a devolver 500/502 de forma persistente (05–07/09); OPeNDAP do mesmo THREDDS segue funcional; ERDDAP continua fora do ar. Os dois `.nc` semestrais órfãos de D10 em `data/raw/pilot/oisst` foram mantidos (imutabilidade do raw) mas não são mais lidos | ativa |
| D14 | 2026-09-07 | **Decisão do pesquisador sobre a viabilidade: RESTRINGIR (continuar com ajustes)** — costa inteira como escopo principal; PPML (`fepois`) como especificação principal + universo *fishing footprint*; comparar H3 res 4 vs 5 na amostra completa; exposição contínua (dias-MHW/intensidade); população declarada = frota rastreada por AIS (dominada por espinhel); ler ToS da API GFW antes de publicar agregados. Portão do `_targets.R` liberado | Q5 reprovada (5,2% células-mês com esforço>0 no NE) e Q9 com alerta (zero artesanal), conforme regra de decisão pré-registrada do feasibility_report v1.0 | ativa |
| D15 | 2026-09-07 | OISST do estudo completo via **arquivos anuais globais da PSL** (`downloads.psl.noaa.gov`, ~476 MB/ano, 1991–2024 ≈ 16 GB) com recorte local ao bbox e cache em `data/interim/oisst_crop`; globais mantidos em `data/raw/oisst_global` com checksum | OPeNDAP limita respostas a ~1–2 MB (D13) — inviável para ~19 mil pixels da costa inteira; NCSS/ERDDAP quebrados; disco (1,3 TB livres) e banda (~8 MB/s → ~1 min/arquivo) comportam | ativa |
| D16 | 2026-09-07 | Período do estudo completo: esforço **2013-01 a 2024-12** (12 anos completos; descarta 2012, ano de ramp-up do AIS); SST 1991–2024 (climatologia 1991–2020 + análise) | Compatibilidade GFW∩OISST com margem, anos completos evitam sazonalidade truncada | ativa |
