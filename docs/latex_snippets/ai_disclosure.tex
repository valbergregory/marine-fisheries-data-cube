% ai_disclosure.tex — paste into main.tex immediately before \bibliography (Elsevier
% placement) or inside the Methods/Acknowledgements section (Springer, Wiley, OUP, CUP,
% T&F, SAGE). Fill every [bracket]. Keep only ONE language block.
% The same text goes into the submission form and the cover letter.

% ---------- English ----------
\section*{Declaration of generative AI and AI-assisted technologies}
During the preparation of this work the author used Claude Code (Anthropic; model
Claude Fable 5.1; desktop application version [X.Y.Z]) to assist in writing, testing
and documenting the data-collection, data-processing and estimation code released in
the public repository, and to prepare the reproducibility documentation of that
repository. The author designed the study, specified every procedure, reviewed and
executed all code, and verified all outputs.
% Keep the next sentence only if an LLM is part of the METHOD (articles 3, 4, 5):
An open-weights language model ([name, version, hash]) was used as a measurement
instrument, as described in Section~\ref{sec:methods}; its outputs were validated
against human annotation and did not replace the author's analysis.
No generative AI tool was used to write, draft or paraphrase the text of this article.
% Keep the next sentence only if a language-editing tool was used on author-written text:
A language-editing tool ([name, version]) was used to check grammar and style of
author-written text.
After using these tools the author reviewed and edited the content as needed and takes
full responsibility for the content of the article.

% ---------- Português ----------
% \section*{Declaração sobre o uso de inteligência artificial generativa}
% Na preparação deste trabalho o autor utilizou o Claude Code (Anthropic; modelo Claude
% Fable 5.1; aplicativo versão [X.Y.Z]) como apoio à escrita, ao teste e à documentação
% do código de coleta, tratamento e estimação disponibilizado no repositório público, e
% à documentação de reprodutibilidade desse repositório. A concepção do estudo, a
% especificação de cada procedimento, a revisão e a execução de todo o código e a
% verificação de todos os resultados foram realizadas pelo autor.
% % Manter só se um LLM integra o MÉTODO (artigos 3, 4, 5):
% Um modelo de linguagem de pesos abertos ([nome, versão, hash]) foi empregado como
% instrumento de medida, conforme descrito na Seção~\ref{sec:methods}; seus resultados
% foram validados contra anotação humana e não substituíram a análise do autor.
% Nenhuma ferramenta de IA generativa foi usada para redigir, esboçar ou parafrasear o
% texto deste artigo.
% % Manter só se houve revisão linguística automática de texto escrito pelo autor:
% Uma ferramenta de revisão linguística ([nome, versão]) foi usada para verificar
% gramática e estilo de texto escrito pelo autor.
% Após o uso dessas ferramentas o autor revisou e editou o conteúdo conforme necessário
% e assume integral responsabilidade pelo conteúdo do artigo (Portaria CNPq
% 2.664/2026, art.~9\textsuperscript{o}, I, ``c'' e ``d'').
